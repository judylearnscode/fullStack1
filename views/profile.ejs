<!doctype html>
<html>
<head>
    <title>Happy Moments Tracker</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="style2.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Koulen&family=Mukta:wght@400;700&display=swap" rel="stylesheet">
    <script src="https://kit.fontawesome.com/5429a7e2cf.js" crossorigin="anonymous"></script>
</head>
<body>
  <% if(lines === undefined){lines = ""} %>
<div class="container">

    <div class="page-header text-center">
        <h1>Happy Moments Tracker</h1>
        <a href="/profile" class="btn btn-default btn-sm deleteAuthorLines">Delete Your Moments</a>
        <a href="/logout" class="btn btn-default btn-sm">Log Out</a>
    </div>
    <div>
        <p>It's easy to forget to remember the happy moments in your life. </p>
          
          <p>Keep a list of all of the moments that bring you joy.</p>
    </div>

    <div class="row">

        <!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
          <h3>Moments</h3>
          <ul class="lines">
          <% for(var i=0; i<lines.length; i++) {%>
            <li class="message">
              <span><%= lines[i].name %></span>
              <span><%= lines[i].line %></span>
              <span><%= lines[i].thumbUp %></span>
              <span><i class="fa-solid fa-face-smile" aria-hidden="true"></i></span>
            </li>
          <% } %>
          </ul>
          
        <% if (user.local.email) { %>
          <form action="/postLine" method="POST">
            <input type="text" placeholder="Name" name="name">
            <input type="text" placeholder="Happy Moment" name="line">
            <button type="submit">Submit</button>
          </form>
        </div>
        <% } %>

</div>

<script src="main.js"></script>

</body>
</html>
